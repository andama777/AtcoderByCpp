/*stdc++.h でエラーが出た　よくわからん*/
#include <bits/stdc++.h>
using namespace std;

// #define rep(i, n) for (int i = 0; i < (int)(n); i++) // macro for * n
// #define rep2(i, s, n) for (int i = (s); i < (int)(n); i++) // for i in range(s : n - 1)

int main(){
    int a;
    cin >> a;

    int b,c;
    cin >> b >> c;

    string s;
    cin >> short
    cout << (a + b + c) << " " << s << endl;
    return 0;
}